---
import { formatCalendarDate } from '../lib/content-dates.mjs';

interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{formatCalendarDate(date)}
</time>
