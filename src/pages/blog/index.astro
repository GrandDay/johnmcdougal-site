---
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';
import Header from '../../components/Header.astro';
import { SITE_TITLE } from '../../consts';
import { getSeriesGroups } from '../../lib/series';

const pageDescription =
	'Essays and technical notes on systems, documentation, AI workflows, Cloudflare, email infrastructure, and building johnmcdougal.com.';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
const seriesGroups = getSeriesGroups(posts);

const tagMap = new Map<string, number>();
posts.forEach((post) => {
	post.data.tags.forEach((tag) => {
		tagMap.set(tag, (tagMap.get(tag) ?? 0) + 1);
	});
});
const allTags = [...tagMap.entries()].sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0]));
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`Writing — ${SITE_TITLE}`} description={pageDescription} />
	</head>
	<body>
		<Header />
		<main>
			<h1>Writing</h1>
			<p class="lead">Essays, walkthroughs, and thinking out loud. Process over polish.</p>

			{allTags.length > 0 && (
				<div class="tag-bar">
					{allTags.map(([tag]) => (
						<a href={`/tags/${tag}`} class="tag">{tag}</a>
					))}
				</div>
			)}

			{posts.length === 0 && (
				<p style="color: var(--text-muted)">Nothing published yet — something is always in the draft pile. Check back soon.</p>
			)}

			<section class="series-organizer" aria-labelledby="series-heading" data-series-organizer>
				<div class="section-heading">
					<h2 id="series-heading">Series</h2>
					<p>Follow each collection in order, or continue to the complete chronological index.</p>
				</div>
				<div class="series-list">
					{seriesGroups.map(({ key, definition, members }) => (
						<article class="series-entry" data-series-group={key}>
							<div class="series-entry-heading">
								<h3>{definition.label}</h3>
								<span>{members.length} posts</span>
							</div>
							<p>{definition.description}</p>
							<ol>
								{members.map((post) => (
									<li data-series-member={post.id}>
										<span>Part {post.data.seriesPart}</span>
										<a href={`/blog/${post.id}/`}>{post.data.title}</a>
									</li>
								))}
							</ol>
						</article>
					))}
				</div>
			</section>

			<section aria-labelledby="all-writing-heading" data-chronological-index>
				<div class="section-heading all-writing-heading">
					<h2 id="all-writing-heading">All writing</h2>
					<p>Every post in reverse chronological order.</p>
				</div>
				<ul class="post-list">
					{posts.map((post) => (
						<li data-chronological-post={post.id}>
							<a href={`/blog/${post.id}/`} class="post-card">
								<div class="post-header">
									<h3>{post.data.title}</h3>
									{post.data.series && post.data.seriesPart && (
										<span class="series-badge">Part {post.data.seriesPart}</span>
									)}
								</div>
								{post.data.description && (
									<p class="post-desc">{post.data.description}</p>
								)}
								<div class="post-meta">
									<span class="date"><FormattedDate date={post.data.pubDate} /></span>
									{post.data.tags.length > 0 && (
										<div class="post-tags">
											{post.data.tags.map((tag) => (
												<span class="tag-label">{tag}</span>
											))}
										</div>
									)}
								</div>
							</a>
						</li>
					))}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	h1 { margin-bottom: 0.25em; }
	.lead {
		color: var(--text-muted);
		margin-bottom: 1.5em;
		font-size: 1.05em;
	}
	.tag-bar {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5em;
		margin-bottom: 2em;
		padding-bottom: 1.5em;
		border-bottom: 1px solid var(--border);
	}
	.section-heading {
		margin-bottom: 1em;
	}
	.section-heading h2 {
		font-size: 1.35em;
		margin-bottom: 0.2em;
	}
	.section-heading p {
		margin: 0;
		color: var(--text-muted);
		font-size: 0.95em;
	}
	.series-organizer {
		margin-bottom: 3em;
	}
	.series-list {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		border-top: 1px solid var(--border);
		border-bottom: 1px solid var(--border);
	}
	.series-entry {
		padding: 1.25em 1.5em 1.5em 0;
		min-width: 0;
	}
	.series-entry + .series-entry {
		padding-left: 1.5em;
		padding-right: 0;
		border-left: 1px solid var(--border);
	}
	.series-entry-heading {
		display: flex;
		align-items: baseline;
		justify-content: space-between;
		gap: 1em;
		margin-bottom: 0.45em;
	}
	.series-entry-heading h3 {
		font-size: 1.05em;
		margin: 0;
	}
	.series-entry-heading span {
		flex: 0 0 auto;
		font-size: 0.75em;
		color: var(--text-muted);
	}
	.series-entry > p {
		margin: 0 0 1em;
		color: var(--text-muted);
		font-size: 0.9em;
		line-height: 1.55;
	}
	.series-entry ol {
		list-style: none;
		padding: 0;
		margin: 0;
	}
	.series-entry li {
		display: grid;
		grid-template-columns: 3.6em minmax(0, 1fr);
		gap: 0.75em;
		align-items: center;
		min-height: 44px;
		border-bottom: 1px solid rgba(128,128,128,0.1);
	}
	.series-entry li:last-child {
		border-bottom: 0;
	}
	.series-entry li > span {
		font-size: 0.72em;
		font-weight: 700;
		color: var(--text-muted);
		text-transform: uppercase;
	}
	.series-entry a {
		display: flex;
		align-items: center;
		min-height: 44px;
		color: var(--text);
		font-size: 0.9em;
		line-height: 1.35;
	}
	.series-entry a:hover,
	.series-entry a:focus-visible {
		color: var(--accent);
		text-decoration: underline;
	}
	.all-writing-heading {
		margin-bottom: 1em;
	}
	.post-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 1px;
		border: 1px solid var(--border);
		border-radius: 8px;
		overflow: hidden;
	}
	.post-card {
		display: block;
		padding: 1.25em 1.5em;
		background: var(--bg-secondary);
		text-decoration: none;
		color: var(--text);
		transition: background 0.15s ease;
	}
	.post-card:hover {
		background: var(--bg);
	}
	.post-card:hover h3 {
		color: var(--accent);
	}
	.post-header {
		display: flex;
		align-items: center;
		gap: 0.75em;
		margin-bottom: 0.25em;
	}
	.post-header h3 {
		font-size: 1.1em;
		margin: 0;
		transition: color 0.15s ease;
	}
	.post-desc {
		margin: 0 0 0.6em;
		color: var(--text-muted);
		font-size: 0.95em;
	}
	.post-meta {
		display: flex;
		align-items: center;
		gap: 1em;
		flex-wrap: wrap;
	}
	.date {
		font-size: 0.8em;
		color: var(--text-muted);
	}
	.post-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.4em;
	}
	.tag-label {
		font-size: 0.7em;
		color: var(--text-muted);
		background: var(--bg);
		padding: 0.15em 0.5em;
		border-radius: 999px;
		border: 1px solid var(--border);
	}
	@media (max-width: 640px) {
		.series-list {
			display: block;
		}
		.series-entry,
		.series-entry + .series-entry {
			padding: 1.1em 0 1.25em;
			border-left: 0;
		}
		.series-entry + .series-entry {
			border-top: 1px solid var(--border);
		}
	}
	@media (max-width: 420px) {
		.series-entry-heading {
			align-items: flex-start;
			flex-direction: column;
			gap: 0.2em;
		}
		.series-entry li {
			grid-template-columns: 3.25em minmax(0, 1fr);
			gap: 0.55em;
		}
		.post-card {
			padding: 1em;
		}
		.post-header {
			align-items: flex-start;
			flex-direction: column;
			gap: 0.4em;
		}
	}
</style>
